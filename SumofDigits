#include<stdio.h>

int main()
{
    int n;
    printf("Enter the number");
    scanf("%d", &n);
    int r = sum(n);
    printf("Sum of digits is %d\n",r );
    return 0;
}
int sum(int n)
{
    if (n == 0)
       return 0;
    return (n % 10 + sum(n / 10));
}
 
